name: Build and Push to ECR

on:
  push:
    branches:
      - main   # run workflow on push to main branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repo
      - name: Check out code
        uses: actions/checkout@v4

      # Step 2: Configure AWS credentials
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      # Step 3: Login to AWS ECR
      - name: Login to Amazon ECR
        id: login-ecr
        run: |
          aws ecr get-login-password --region ${{ secrets.AWS_REGION }} \
          | docker login --username AWS --password-stdin \
            4300-0637-6054.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com

      # Step 4: Build and push Docker image
      - name: Build, Tag, and Push Docker image
        run: |
          IMAGE_REPO_NAME=fastapi-app
          IMAGE_TAG=latest
          ACCOUNT_ID=4300-0637-6054

          # Build Docker image
          docker build -t $IMAGE_REPO_NAME:$IMAGE_TAG .

          # Tag image for ECR
          docker tag $IMAGE_REPO_NAME:$IMAGE_TAG \
            $ACCOUNT_ID.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/$IMAGE_REPO_NAME:$IMAGE_TAG

          # Push to ECR
          docker push $ACCOUNT_ID.dkr.ecr.${{ secrets.AWS_REGION }}.amazonaws.com/$IMAGE_REPO_NAME:$IMAGE_TAG
